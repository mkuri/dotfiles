# Plan Document Policy

## Design docs (`*-design.md`)

- Commit to git (as brainstorming skill specifies)
- Keep permanently as a record of design decisions

## Implementation plans (`*-plan.md`)

- Do NOT commit to git
- Used as working documents during implementation only
- Delete from the worktree during `finishing-a-development-branch`
